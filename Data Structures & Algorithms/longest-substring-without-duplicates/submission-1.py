class Solution:
    def lengthOfLongestSubstring(self, s: str) -> int:
#        char=list(s)
#        L, total=0,0 #total = size of the sliding window
#        length=0
#        hashset=set()
#        for R in range(len(s)):
#         total+=s[R]



        
#         while char[R] in hashset and range(L,R-1):
#                 length=max(R-L+1, length)
#                 total-=s[L]
#                 L+=1
#         return 0 if length==float('inf') else length
    
        return 0
             